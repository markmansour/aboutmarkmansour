# Expose Astro dependencies for `pnpm` users
shamefully-hoist=true

# @astrolib/* and astro-embed pin older Astro peer ranges that haven't been
# updated for Astro 7 yet. They work fine; this lets `npm ci` resolve them
# without erroring (mirrors `npm install --legacy-peer-deps`).
legacy-peer-deps=true
